# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

.vercel
.env*.local

# Chrome extension build artifact (regenerate with chrome-extension/pack.js)
ytubviral-extension.zip

# Stray backup files (blog-generator etc.)
*.bak

# Campana de feedback (24/08/2026): contienen correos de usuarios reales.
# El repo es PUBLICO -> nunca commitear.
local-agent/feedback-campaign-*.json

# Notas y capturas de revision manual (25/08/2026). Son scratch local: incluyen
# paginas guardadas del navegador con bundles de terceros y capturas del panel
# con el email de admin a la vista. El repo es PUBLICO -> nunca commitear.
Issues/

# Design handoff para el equipo externo: se entrega en ZIP (respaldo en D:\),
# nunca por git. Ver memoria project_design_handoff.
Diseño humano y orgánico/
