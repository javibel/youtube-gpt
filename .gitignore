# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

.vercel
.env*.local

# Chrome extension build artifact (regenerate with chrome-extension/pack.js)
ytubviral-extension.zip

# Duplicado suelto y desactualizado del zip anterior (nombre sin guion, no lo
# genera ningún script) — se dejó de trackear el 2026-09-12, ver memoria.
ytubviralextension.zip

# Stray backup files (blog-generator etc.)
*.bak

# local-agent/ es un junction a C:\Users\jimen\youtube-gpt\local-agent, que tiene su
# propio repo privado (javibel/ytubviral-agent). Se dejó de versionar aquí el 2026-09-13:
# el mismo código se publicaba en este repo PUBLICO (y con él una clave de Tumblr).
# OJO: nunca `git rm -r local-agent` sin --cached — borraría el agente real vía el junction.
/local-agent/

# Notas y capturas de revision manual (25/08/2026). Son scratch local: incluyen
# paginas guardadas del navegador con bundles de terceros y capturas del panel
# con el email de admin a la vista. El repo es PUBLICO -> nunca commitear.
Issues/

# Design handoff para el equipo externo: se entrega en ZIP (respaldo en D:\),
# nunca por git. Ver memoria project_design_handoff.
Diseño humano y orgánico/
